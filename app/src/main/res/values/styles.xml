<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Base.Theme.AppCompat">-->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!--<item name="windowActionBar">false</item>-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.CustomTheme" >
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
    <style name="splash" parent="Theme.AppCompat.DayNight.NoActionBar"/>

    <string-array name="person_types">
        <item>Parent</item>
        <item>Tuteur</item>

    </string-array>
    <string-array name="Organisation">
        <item>Organisation 1</item>
        <item>Organisation 2</item>

    </string-array>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BackGroundImageView">
        <item name="android:alpha">0.5</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:src">@drawable/logosplach</item>
    </style>
</resources>
